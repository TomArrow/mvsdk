if(NOT InMVSDK)
	message(FATAL_ERROR "Use the top-level cmake script!")
endif(NOT InMVSDK)

file(GLOB MVSHAREDMODHEADERS "${SRCDir}/shared/mod/*.h")
file(GLOB MVSHAREDMODSOURCES "${SRCDir}/shared/mod/*.c")

set(MVSHAREDMODHEADERS ${MVSHAREDMODHEADERS} PARENT_SCOPE)
set(MVSHAREDMODSOURCES ${MVSHAREDMODSOURCES} PARENT_SCOPE)
